describe('Home Page', () => {

    it('should display an about page and an option to search', ()=>{
        expect(true).to.equal(true)
        //cy.visit('http://localhost:4200/');
       // cy.contains("Proper Property");
    });
});